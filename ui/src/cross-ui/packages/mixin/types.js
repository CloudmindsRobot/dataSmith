import {isFunction, isString, isDefined, isNumber, isArray, isObject, isDate, isBoolean} from '../utils/types'

export default {
    methods: {
        isFunction,
        isString,
        isDefined,
        isNumber,
        isArray,
        isObject,
        isDate,
        isBoolean
    }
}